{
  "name": "www",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.13.0",
  "scripts": {
    "dev:tv-app": "pnpm --filter tv-app dev",
    "dev:embed": "pnpm --filter embed dev",
    "build": "pnpm -r build",
    "build:tv-app": "pnpm --filter tv-app build",
    "build:embed": "pnpm --filter embed build"
  }
}
